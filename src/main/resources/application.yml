spring:
  datasource:
    url: jdbc:postgresql://webserver:5432/vaultpay
    username: vaultuser
    password: vaultpass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  application:
    name: vaultpay

server:
  port: 8080
